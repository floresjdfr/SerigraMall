import { Table } from "react-bootstrap"

export default function CheckoutItemsList({ children }) {
    return
    <>
        <h1>Shoppig Cart</h1>
        <Table>
            {children}
        </Table>
    </>

} 